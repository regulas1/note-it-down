export const enum TableNames {
    Site = "Site",
    Note = "Note"
}

export const enum SiteColumns {
    Id = "id",
    Url = "url",
    Title = "title"
}

export const enum NoteColumns {
   Id = "id",
   Note = "note",
   SiteId = "siteId",
   Scroll = "scroll"
}